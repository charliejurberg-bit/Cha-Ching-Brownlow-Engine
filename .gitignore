# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# R
.RData
.Rhistory

# Streamlit
.streamlit/secrets.toml

# Data files (optional: remove these lines if you want data on GitHub)

# Offline analysis report output. Regenerated by accuracy_report.py and
# betting_edge_report.py from tracked inputs (predictions/backtest_game_level.csv,
# predictions/backtest_results.csv). Nothing reads them back.
accuracy_report.csv
accuracy_report_insample.csv
accuracy_report_oos.csv
betting_edge_report.csv

# Owned by the cha-ching-brownlow repo (the Next.js front end at
# C:\Users\charl\web\cha-ching-brownlow), tracked there since b12873d and cited
# by app/page.tsx and two components. Any copy in this repo is debris. One was
# cherry-picked in here on 2 August 2026 and reverted the same day; an earlier
# stray copy was a corrupted paste with runaway indentation. Ignored so neither
# happens again unnoticed.
landing_spec.md

# OS
.DS_Store
Thumbs.db

betfair_debug.html
espn_debug*.html

# Local Claude settings (may contain credentials)
# Claude Code workspace config — local tooling, not project source
.claude/

# Debug artifacts
*_debug*.html
*_debug*.py
debug_*.py

# Generated post drafts (unreviewed)
drafts/

# Claude Code local config and scratch scripts
_tmp/

# Local editor config
.vscode/

# Selenium download scratch for the Wheelo match-stats fetch. Cleared at the
# start of every round fetch; the parsed result lands in wheelo_2026.csv.
data_wheelo/downloads/
